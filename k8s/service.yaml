apiVersion: v1
kind: Service
metadata:
  name: catalog-api
  namespace: fcgames
  labels:
    app: catalog-api
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 5002
    name: http
  selector:
    app: catalog-api
